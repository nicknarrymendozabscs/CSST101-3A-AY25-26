// This file contains JavaScript code that adds interactivity to the portfolio.
// You can include functions for handling events, animations, or dynamic content loading.

document.addEventListener('DOMContentLoaded', () => {
    // Example: Add a click event listener to a button
    const button = document.getElementById('exampleButton');
    if (button) {
        button.addEventListener('click', () => {
            alert('Button clicked!');
        });
    }

    // Additional interactivity can be added here
});